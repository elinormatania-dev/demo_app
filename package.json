{
  "name": "billing-dashboard",
  "scripts": {
    "dev": "concurrently -n JS,PYTHON,CLIENT \"npm run dev:js\" \"npm run dev:py\" \"npm run dev:client\"",
    "dev:js":      "node backend-js/index.js",
    "dev:py":      "cd backend-py && venv/bin/uvicorn main:app --reload --port 8000",
    "dev:client":  "cd frontend && npm run dev",
    "fresh":       "rm -f backend-js/cache/*.json && npm run dev",
    "install:all": "npm install && cd frontend && npm install && cd ../backend-js && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
